# Markdown Link Checker

A standalone Python program that extracts links from Markdown files and optionally checks HTTP and HTTPS links for reachability.

## Usage

```bash
python markdown_link_checker.py README.md
python markdown_link_checker.py README.md --check
```

The first command lists links. The second command checks web links and reports their status as JSON.
